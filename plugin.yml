name: Mise Buildkite Plugin
description: Install mise, run `mise install`, and export the tool environment for Buildkite steps.
author: https://github.com/buildkite-plugins
requirements:
  - curl
  - tar
configuration:
  additionalProperties: false
  properties:
    version:
      type: string
      default: latest
      description: Version of mise to install (for example, 2026.2.11).
    dir:
      type: string
      description: Directory where `mise install` and `mise env` run. Defaults to the checkout directory.
    cache-dir:
      type: string
      description: Directory to use for `MISE_DATA_DIR`. Useful for persistent caches on self-hosted agents.
    install_args:
      type: string
      description: Arguments to pass to `mise install` (for example, node pnpm or node@24 pnpm@10). These are install-only arguments; command execution still uses the environment exported from the repository mise config.
    disable_tools:
      type: array
      items:
        type: string
        minLength: 1
      description: Tools from the repository mise config to ignore. Sets `MISE_DISABLE_TOOLS` as a comma-separated list.
    install_system_packages:
      type: boolean
      default: false
      description: Run `mise system install --yes` before `mise install` to install configured `[system.packages]`.
